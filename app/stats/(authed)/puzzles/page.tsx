import { Stub } from "../../_components-stub";

export const dynamic = "force-dynamic";

export default function PuzzlesStatsPage() {
  return (
    <Stub
      title="Puzzles"
      body="Per-puzzle breakdown, hardest/easiest, today's tier split. The Overview page still has them for now."
    />
  );
}
