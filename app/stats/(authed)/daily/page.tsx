// Daily stats — 14-day chart, moves distribution. Stub for now;
// content will move out of /stats Overview as the split progresses.

import { Stub } from "../../_components-stub";

export const dynamic = "force-dynamic";

export default function DailyStatsPage() {
  return <Stub title="Daily" body="14-day activity chart and moves distribution will live here. The Overview page still has them for now." />;
}
