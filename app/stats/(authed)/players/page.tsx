import { Stub } from "../../_components-stub";

export const dynamic = "force-dynamic";

export default function PlayersStatsPage() {
  return (
    <Stub
      title="Players"
      body="Returning rate, top player, language breakdown, hint-toggle prefs. The Overview page still has them for now."
    />
  );
}
