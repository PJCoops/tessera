import { Stub } from "../../_components-stub";

export const dynamic = "force-dynamic";

export default function CohortsStatsPage() {
  return (
    <Stub
      title="Cohorts"
      body="Weekly cohort retention table — D1 / D3 / D7 / D14 / D30. The Overview page still has it for now."
    />
  );
}
