import { Stub } from "../../_components-stub";

export const dynamic = "force-dynamic";

export default function NotificationsStatsPage() {
  return (
    <Stub
      title="Notifications"
      body="Email + push subscriber counts, daily push funnel (received / clicked / click-through). The Overview page still has them for now."
    />
  );
}
