import 'package:flutter/material.dart';

/// Placeholder — the real "Today's words" screen (en definitions with
/// fallback, es word-list-only) lands in a later Phase 3 slice.
class WordsScreen extends StatelessWidget {
  const WordsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Words')));
  }
}
